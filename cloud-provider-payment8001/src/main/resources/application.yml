server:
  port: 8001

spring:
  application:
    name: cloud-payment-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/cloud2020?serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: liaowh123

mybatis:
  mapperLocations: classpath:mapper/*.xml
  type-aliases-package: com.liaowh.springcloud.entities    # 所有Entity别名类所在包
